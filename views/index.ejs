<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Save Details</title>
    <style>
        html {
            height: 100%;
        }
        body {
            margin: 0;
            padding: 0;
            font-family: sans-serif;
            background: linear-gradient(white, #3d4e61);
            color: #fff;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .login-box {
            width: 400px;
            padding: 40px;
            background: rgba(0, 0, 0, 0.5);
            box-sizing: border-box;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.6);
            border-radius: 10px;
            text-align: center;
        }

        .login-box h2 {
            margin: 0 0 30px;
            padding: 0;
            color: #fff;
            text-align: center;
            font-size: 24px;
        }

        .login-box .user-box {
            position: relative;
        }

        .login-box input {
            width: 100%;
            padding: 10px 0;
            font-size: 16px;
            color: #fff;
            margin-bottom: 30px;
            border: none;
            border-bottom: 1px solid #fff;
            outline: none;
            background: transparent;
        }

        .login-box label {
            position: absolute;
            top: 0;
            left: 0;
            padding: 10px 0;
            font-size: 16px;
            color: #fff;
            pointer-events: none;
            transition: .5s;
        }

        .login-box input:focus ~ label,
        .login-box input:valid ~ label {
            top: -20px;
            left: 0;
            color: #0e0f0f;
            font-size: 12px;
        }

        .login-box button[type="submit"] {
            position: relative;
            background: #0d0d0d;
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            text-transform: uppercase;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            overflow: hidden;
        }

        .login-box button[type="submit"]:hover {
            background: transparent;
            color: #0d0e0e;
        }
        /* Add the following styles for the header in your existing CSS */

/* Updated styles for the header */
header {
    background-color: #3d4e61;
    padding: 15px;
    text-align: center;
    width: 100%; /* Occupy the entire width */
    position: fixed; /* Fix the header at the top */
    top: 0;
}

header h2 {
    margin: 0;
    color: #fff;
}

nav {
    margin-top: 10px;
}

nav a {
    color: #fff;
    text-decoration: none;
    margin: 0 10px;
    font-size: 16px;
}

nav a:hover {
    text-decoration: underline;
}
footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 0.5px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        
    </style>
</head>
<body>
    <header>
        <h2>save Details</h2>
        <nav>
            <a href="/">Home</a>
            <a href="/contactus">Contact Us</a>
            <a href="/profile">Profile</a>
        </nav>

    

    </header>
    <div class="login-box">
        <form id="saveDetailsForm" action="/save" method="post">
            <div class="user-box">
                <input type="hidden" name="application_no" value="<%= user.woxsenID %>">
            </div>
            <div class="user-box">
                <input type="number" name="number_of_clothes" required max="8">
                <label>Number of Clothes</label>
            </div>
            <button type="submit">Save</button>
            
        </form>
    </div>
    <footer>
        <p>&copy; 2023  All rights reserved.</p>
    </footer>
</body>
</html>